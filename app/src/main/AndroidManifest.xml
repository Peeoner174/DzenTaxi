<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.peeoner174.msi.oneapril2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".driver.activity.UserInfoActivity"

            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".driver.activity.TapleActivity"
            android:label="Лента"
            android:theme="@style/AppTheme.NoActionBar"
        />

        <activity android:name=".driver.activity.MapsActivity"
            android:label="Карта"
            android:theme="@style/AppTheme.NoActionBar"
        />

        <activity android:name=".driver.activity.MainGalleryActivity"
            android:label="Галерея"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".driver.activity.DetailGalleryActivity"
            android:label="Подробно"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".driver.activity.NoteActivity"
            android:label="Заметки"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDmGe1fZdGOTtznzB1kd9LMkvN2VfH887c" />
    </application>

</manifest>